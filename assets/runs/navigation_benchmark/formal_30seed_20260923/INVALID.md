# INVALID - do not use for paper statistics

This batch was launched with `--headless`. In this repository that sets
`graphics_device_id=-1`, so Isaac Gym cannot create the depth camera. The
planner consequently received an empty BEV. These trials are retained only
for auditing the failure.
